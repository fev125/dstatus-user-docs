# DStatus 用户文档仓规则

## 定位与真源

- 本仓只承载面向终端用户的文档和静态资源，不写开发 SOP、维护过程、主仓路径或给 Agent 的旁白。
- 长期维护与 changelog 口径以主仓 `../docs/用户文档/长期口径.md` 为唯一真源；该入口不可用时，先核对当前产品与发布状态，不猜测。

## 文档约束

- 只描述已经上线或有当前证据的能力，不发明未发布功能，不把计划写成现状。
- changelog 在用户明确要求 push 前更新用户向增量（含被控功能增加）；禁止“待发”占位段。口径只认主仓长期口径。
- 导航以 `SUMMARY.md`、`index.md` 和 `README.md` 为入口；新增、改名或删除页面时同步核对导航。
- 用户可见内容保持中文、精简、结果导向；不得出现真实 IP、密钥、口令、默认密码或内部发布细节。
