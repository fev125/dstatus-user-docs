# Cloudflare Pages 配置

## ⚠️ 正确的配置参数

在 Cloudflare Pages 构建设置中，请填写以下参数：

### 构建配置
```
Production branch:           main
Build command:               npm run build
Build output directory:      .vitepress/dist
Node version:                20
Root directory:              (留空或 /)
```

## 🔍 常见错误

### ❌ 错误配置
```
Build command: .vitepress/dist  ← 这是错误的！
```

### ✅ 正确配置
```
Build command: npm run build
Build output directory: .vitepress/dist
```

## 📋 完整配置步骤

1. **Framework preset**: 选择 `VitePress`（会自动填充正确配置）
   
2. **Build command**: `npm run build`
   - 这是构建命令，用于生成静态文件
   
3. **Build output directory**: `.vitepress/dist`
   - 这是输出目录，告诉 Cloudflare 从哪里读取静态文件
   
4. **Environment variables** (可选):
   - `NODE_VERSION`: `20`
   
5. **Root directory**: 留空（默认为仓库根目录）

## 🚀 推荐配置方式

### 方式一：使用框架预设（最简单）

1. 在 Framework preset 下拉菜单中选择 **VitePress**
2. Cloudflare 会自动填充：
   - Build command: `npm run build`
   - Build output directory: `.vitepress/dist`
3. 点击 **Save and Deploy**

### 方式二：手动配置

如果没有 VitePress 预设，手动填写：

| 字段 | 值 |
|------|-----|
| Build command | `npm run build` |
| Build output directory | `.vitepress/dist` |
| Node version | `20` |

## 🔧 故障排查

如果仍然失败，检查：

1. ✅ `package.json` 中是否有 `build` 脚本
2. ✅ 依赖是否正确（运行 `npm install` 测试）
3. ✅ 本地构建是否成功（运行 `npm run build`）

## 📸 配置截图参考

```
┌─────────────────────────────────────┐
│ Framework preset                    │
│ ▼ VitePress                         │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Build command                       │
│ npm run build                       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Build output directory              │
│ .vitepress/dist                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Root directory (optional)           │
│ /                                   │
└─────────────────────────────────────┘
```



