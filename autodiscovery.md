# 自动发现（批量安装 Agent）

## 入口

管理后台 → 自动发现（`/admin/autodiscovery`）

## 你能用它做什么

- 生成安装命令：把 Agent 装到被控机上
- 选择通信模式：主动上报 / 被动（面板主动拉取）
- 审核/批准：如果你开启了审核，新节点需要你手动批准才能进入列表

## 推荐用法（最短路径）

1. 打开自动发现，选择目标系统。
2. 确认通讯方式和安装授权有效期，需要修改时打开“设置”。未设置过通讯偏好时默认主动上报；被动模式需确认 Agent 端口可达，默认端口为 `9999`。
3. 点击“复制”生成安装命令，在授权有效期内到目标设备执行。同一授权可用于多台设备，每台设备使用独立通讯密钥。
4. 回到“接入节点”查看结果；开启接入审核时，在“待审核”中批准。

安装授权默认有效 30 分钟，也可设置为永久。授权过期或被撤销后，原安装命令不能继续领取节点身份和密钥，需要重新生成。撤销安装授权不等于撤销已接入节点的通讯密钥，已有节点可继续正常通信。

## 什么时候用主动上报

当面板无法访问被控机的通信端口时（例如被控机在 NAT/内网里），用主动上报。

主动上报需要被控机能够访问主控的上报地址；被动模式需要主控能够访问被控机的 Agent 端口。

不确定怎么选时，按“哪一端能连哪一端”来选即可，见：[使用指南](/usage) 的“模式怎么选”。

## 常见问题

### 执行了安装命令，但节点没有出现

- 先等一会再刷新页面（新节点注册需要一点时间）
- 用“服务器管理”里的“安装探针/卸载探针”确认命令是否执行成功（见：[服务器管理](/server-management)）

### 节点出现了但显示离线

优先按 Agent 排障清单一步步查（见：[Agent 使用指南](/agent-guide)）

### 旧版被控怎么升级

如果这台被控原来是旧版本，优先看专门教程：

- [被控升级教程（v1.1 -> 最新版）](/agent-upgrade)

这类大跨度升级，默认优先重新执行当前面板生成的升级/重装命令，不要优先手工替换旧文件。
